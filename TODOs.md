# TODOs

TrackPal implementation queue.

## Status Tags

- `[ready]` — eligible for implementation when linked to a non-draft spec.
- `[draft]` — reviewed ticket awaiting spec promotion through `to-ready`.
- `[blocked]` — not eligible until every listed blocker is complete.
- `[in-progress]` — currently being implemented.
- `[done]` — complete; include the commit hash and PR URL when available.

## Queue

- [ready] Make Tenant hard deletion provider-independent
  - Spec: `specs/tenant-account-deletion-and-data-export.md`
  - Blocked by: None — can start immediately
  - Scope: make the existing Master hard-deletion path remove the complete PostgreSQL Tenant graph without depending on Evolution, while preserving the current inactive-only web behavior until the unified confirmation flows are added.
  - Acceptance:
    - Deleting an inactive Tenant removes the owner user, every client user, refresh sessions, and every currently known tenant-scoped row.
    - Shared global service-governance records remain unchanged.
    - Active Tenants remain rejected by the existing route in this prefactoring slice.
    - Evolution and other excluded providers are not called by the hard-deletion transaction.
    - A PostgreSQL failure rolls the deletion back and returns an error rather than reporting success.
    - The deletion-inventory test fails when a newly introduced tenant-scoped model is omitted.
  - Validation:
    - `cd backend && uv run pytest -k "tenant_account_deletion or delete_tenant or tenants"`
    - `cd backend && uv run pytest`
    - Follow the backend style checks in the spec Testing Plan.
  - Notes: preserve the current API surface temporarily; this slice creates the provider-independent database seam used by owner and Master confirmation later.

- [ready] Deliver the owner export tracer bullet
  - Spec: `specs/tenant-account-deletion-and-data-export.md`
  - Blocked by: None — can start immediately
  - Scope: let a Tenant owner open Data and account, request an asynchronous account-only ZIP, observe pending/generating/ready status, and download it through an authorized backend endpoint.
  - Acceptance:
    - Data and account is visible to both Starter and Pro Tenant owners without exposing unrelated Pro-only settings.
    - Owner export routes derive the Tenant exclusively from the authenticated owner and reject Clients, unauthenticated callers, and another owner.
    - Export creation persists an opaque request, queues work, and returns `202 Accepted` with a safe status.
    - The worker reads a consistent snapshot and produces a localized README, manifest, JSON, and CSV for the Account dataset.
    - The first format version uses public localized names and does not expose database table/column names or internal UUIDs.
    - The artifact uploads to the configured R2 bucket under an unpredictable Tenant-separated key.
    - Status and download responses never expose the R2 object key or direct public URL.
    - The ready ZIP is downloadable through the backend while the owner remains authorized.
    - Backend and frontend tests cover the happy path and the primary authorization failures.
  - Validation:
    - `cd backend && uv run pytest -k "tenant_export"`
    - `cd frontend && npm test -- --run -t "Data and account|tenant export"`
    - Follow the remaining backend and frontend gates in the spec Testing Plan.
  - Notes: this is the narrow end-to-end export seam; later tickets expand datasets and harden lifecycle behavior without replacing the route or UI concepts.

- [blocked] Complete the localized basic export contract
  - Spec: `specs/draft-tenant-account-deletion-and-data-export.md`
  - Blocked by: Deliver the owner export tracer bullet
  - Scope: expand the downloadable ZIP into the complete human-oriented basic export covering Account, Clients, Catalog, and Subscriptions in the Tenant's frozen locale.
  - Acceptance:
    - The request freezes the server-side Tenant locale and the ZIP uses it even if the Tenant later changes locale.
    - English and Spanish archives contain localized public file names, JSON properties, CSV headers, category descriptions, and README content.
    - Clients, services, plans, and subscriptions use deterministic export-local relationship IDs rather than internal UUIDs.
    - The manifest reports format version, locale, request ID, generation/snapshot times, included and excluded categories, counts, sizes, and valid checksums.
    - Account, Clients, Services, Plans, and Subscriptions have complete JSON and useful CSV representations with consistent relationships.
    - Password hashes, refresh-token hashes, API keys, Evolution credentials, subscription passwords, profile PINs, mailbox/OAuth/IMAP secrets, settings, histories, sessions, and operational records are absent from every archive entry.
    - Spreadsheet formula prefixes in user-controlled CSV values are neutralized without changing the JSON value.
    - Empty datasets produce valid zero-count files and large datasets are read and serialized with bounded memory.
    - Golden-structure tests cover both supported locales without snapshotting volatile timestamps.
  - Validation:
    - `cd backend && uv run pytest -k "tenant_export"`
    - `cd backend && uv run pytest`
    - Follow the archive inspection and style checks in the spec Testing Plan.
  - Notes: keep the public export schema independent from ORM names so database migrations do not silently change the customer-facing format.

- [blocked] Make export lifecycle durable and self-cleaning
  - Spec: `specs/draft-tenant-account-deletion-and-data-export.md`
  - Blocked by: Complete the localized basic export contract
  - Scope: make export requests safe under concurrency and failures, expose retryable status to the owner, retain ready ZIPs for 72 hours, and remove artifacts and visible request state automatically.
  - Acceptance:
    - At most one current export exists per Tenant under concurrent create requests.
    - Creating while pending, generating, or unexpired ready returns the current request instead of creating a second job.
    - Workers claim a request atomically and competing workers cannot publish duplicate current artifacts.
    - A generation run retries transient failures at most three times with bounded backoff and fails permanent errors immediately.
    - Failed status is safe and localized; the owner can explicitly retry the same current request.
    - Failed and crashed jobs do not publish partial artifacts as ready, and abandoned objects use the lifecycle-managed prefix.
    - Ready ZIPs can be downloaded repeatedly without extending expiry.
    - The artifact and current request record disappear after 72 hours and no expired export history remains visible.
    - No manual user-facing export-delete operation is introduced.
    - Minimal request/generation/download/expiry audit events contain no contents, credentials, object keys, URLs, or request bodies.
    - Export audit events expire after 12 months while the Tenant exists and cascade when the Tenant is deleted.
    - The frontend polls only while pending/generating and supports ready, failed, and retry states.
  - Validation:
    - `cd backend && uv run pytest -k "tenant_export"`
    - `cd frontend && npm test -- --run -t "tenant export"`
    - Follow the concurrency, cleanup, audit, full-suite, and style gates in the spec Testing Plan.
  - Notes: reuse the existing database-job, Redis-queue, lifespan-worker, and periodic-cleanup conventions; publication must recheck that the Tenant and request still exist.

- [blocked] Enable Master basic data export
  - Spec: `specs/draft-tenant-account-deletion-and-data-export.md`
  - Blocked by: Make export lifecycle durable and self-cleaning
  - Scope: let Master request, monitor, retry, and repeatedly download the same basic export for any selected Tenant from the web Tenant-management surface.
  - Acceptance:
    - Master routes authorize the selected Tenant and current export request on every create, status, retry, and download operation.
    - Master uses the same format, worker, attempt limits, audit events, and 72-hour lifecycle as the owner flow.
    - Master export does not require password reauthentication beyond the active session.
    - The Master web surface exposes export status and download without switching into a less-privileged owner identity.
    - Master audit events identify the Master actor and target Tenant without recording export contents or object URLs.
    - Tenant owners and Clients cannot use Master routes or access a different Tenant by changing route/request identifiers.
    - Master export UI errors and success states are localized and do not reveal R2 keys or direct URLs.
  - Validation:
    - `cd backend && uv run pytest -k "tenant_export and master"`
    - `cd frontend && npm test -- --run -t "Master|tenant export"`
    - Follow the full authorization and build gates in the spec Testing Plan.
  - Notes: keep owner routes identifier-free and Master routes explicit rather than sharing a client-supplied target shape.

- [blocked] Deliver owner self-service account deletion
  - Spec: `specs/draft-tenant-account-deletion-and-data-export.md`
  - Blocked by: Make Tenant hard deletion provider-independent; Deliver the owner export tracer bullet
  - Scope: let the authenticated owner permanently close an active Tenant from Data and account after the approved warning, optional export-first choice, password verification, and localized typed phrase.
  - Acceptance:
    - The owner deletion route derives the target Tenant exclusively from the authenticated owner.
    - The two-step modal shows the approved localized warning and offers Export first or Continue without export.
    - Export first starts/returns the current export and returns the user to export status before destructive confirmation.
    - The actor's current password and localized `ELIMINAR MI CUENTA` or `DELETE MY ACCOUNT` phrase are both verified server-side.
    - Phrase validation trims exterior whitespace and ignores case while rejecting other text.
    - Five failed password confirmations in 15 minutes trigger an actor-and-IP deletion-only cooldown without disabling normal login.
    - Active Tenant status does not block valid owner deletion and there is no grace period or cancellation after confirmation.
    - PostgreSQL commit is the success gate; rollback/error keeps the account usable and does not render the final page.
    - Success invalidates owner/client access and refresh tokens through deletion, clears local auth and caches, and renders a public final page with a login button.
    - No email, WhatsApp notice, deletion receipt, deletion audit, or follow-up status is created.
    - Deleted usernames, client prefixes, phone numbers, emails where constrained, and Evolution instance names can be reused immediately.
  - Validation:
    - `cd backend && uv run pytest -k "tenant_account_deletion"`
    - `cd frontend && npm test -- --run -t "Data and account|tenant deletion"`
    - Follow the full backend/frontend and accessibility gates in the spec Testing Plan.
  - Notes: reuse the provider-independent PostgreSQL seam and verify the authenticated actor's password directly rather than invoking normal login side effects.

- [blocked] Make deletion export-aware and clean local ephemeral data
  - Spec: `specs/draft-tenant-account-deletion-and-data-export.md`
  - Blocked by: Make export lifecycle durable and self-cleaning; Deliver owner self-service account deletion
  - Scope: handle every current export state during deletion and make one non-blocking cleanup attempt for attributable Redis and R2 data while preserving PostgreSQL as the only closure gate.
  - Acceptance:
    - A generating export offers Wait for export or Delete now; waiting preserves the job and delete-now cancels/removes it.
    - A failed export offers Retry export or Continue to deletion.
    - A ready but undownloaded ZIP triggers an explicit reminder but does not block deletion.
    - Tenant deletion removes current export and audit database rows and attempts current/partial R2 object deletion.
    - Deletion attempts attributable admin-session and context-key cleanup against both configured Redis stores, not only the active failover target.
    - Redis or R2 cleanup failure is logged safely, is not retried or shown to the user, and does not prevent PostgreSQL commit.
    - Tests prove the accepted residual windows of approximately five minutes for Redis and up to 72 hours for export objects.
    - Deletion makes no Evolution, Google, Microsoft, n8n, or Render call.
    - A duplicate deletion submission is terminal and cannot recreate jobs or side effects.
    - A best-effort opaque backup-suppression marker contains no direct Tenant identifier or business data and is consumable by a restore filter.
    - Suppression-marker failure follows the accepted non-blocking R2 policy.
  - Validation:
    - `cd backend && uv run pytest -k "tenant_export or tenant_account_deletion"`
    - `cd frontend && npm test -- --run -t "tenant export|tenant deletion"`
    - Follow the Redis/R2 failure, restore-filter, full-suite, and manual checks in the spec Testing Plan.
  - Notes: immediate closure means loss of account access after PostgreSQL commit; this slice must preserve the explicitly accepted Redis/R2 TTL fallback and external-provider exclusions.

- [blocked] Unify Master web deletion with owner safeguards
  - Spec: `specs/draft-tenant-account-deletion-and-data-export.md`
  - Blocked by: Enable Master basic data export; Make deletion export-aware and clean local ephemeral data
  - Scope: replace the Master dashboard's simple alert and bypassable direct DELETE operation with the same password, phrase, export-aware, and local-cleanup safeguards used by owner deletion.
  - Acceptance:
    - Master can start deletion for an active or inactive selected Tenant from web.
    - Master confirms with the Master's own current password and the localized destructive phrase.
    - Master receives the same export-first, generating, failed, and ready-artifact choices for the selected Tenant.
    - The administrative route performs explicit Master and target-Tenant authorization before invoking the shared deletion service.
    - The former direct DELETE operation can no longer bypass password and phrase verification.
    - PostgreSQL failure shows an error and preserves the Tenant; success keeps Master authenticated.
    - After success, Master returns to a refreshed Tenant list with a localized confirmation.
    - The deleted Tenant owner receives no email, WhatsApp message, or in-app notification.
    - Cross-role and mismatched Tenant/request attempts fail without disclosing another Tenant's resources.
  - Validation:
    - `cd backend && uv run pytest -k "tenant_account_deletion and master"`
    - `cd frontend && npm test -- --run -t "Master|tenant deletion"`
    - Follow the full authorization, regression, build, and style gates in the spec Testing Plan.
  - Notes: retain Master auth and caches unrelated to the deleted Tenant; only owner deletion navigates to the public final page.

- [blocked] Retire Tenant deletion from Master WhatsApp
  - Spec: `specs/draft-tenant-account-deletion-and-data-export.md`
  - Blocked by: Unify Master web deletion with owner safeguards
  - Scope: remove Tenant deletion from Master WhatsApp menus and make recognized legacy attempts direct Master to the web without performing destructive work.
  - Acceptance:
    - Active and inactive Tenant WhatsApp detail menus no longer present a deletion action.
    - A recognized legacy delete selection or confirmation returns a localized web-dashboard direction.
    - Legacy attempts never call the Tenant deletion service and never change Tenant state.
    - Tests that previously expected WhatsApp deletion are replaced with menu-removal and no-side-effect expectations.
    - Master WhatsApp activation, deactivation, editing, selection, navigation, and universal cancel behavior remain unchanged.
    - Tenant Admin and Client WhatsApp flows remain unchanged.
  - Validation:
    - `cd backend && uv run pytest -k "whatsapp_master_delete_redirect or whatsapp_lifecycle_flow"`
    - `cd backend && uv run pytest`
    - Follow the backend style checks in the spec Testing Plan.
  - Notes: preserve a deliberate legacy response rather than treating old delete input as an unrelated unknown command.

- [blocked] Complete offboarding documentation and release verification
  - Spec: `specs/draft-tenant-account-deletion-and-data-export.md`
  - Blocked by: Retire Tenant deletion from Master WhatsApp
  - Scope: make the completed export/deletion behavior operable and reviewable by synchronizing product, architecture, help, deployment, and restore documentation and executing the full release validation matrix.
  - Acceptance:
    - System, API, database, Redis, frontend, WhatsApp, i18n, business-rule, structure, and user-help documentation matches implemented behavior.
    - Documentation clearly distinguishes the basic export from formal privacy-right requests and lists every excluded dataset/provider.
    - Tenant help explains export scope, repeated downloads, 72-hour expiry, optional export-first deletion, irreversibility, and lack of recovery.
    - Deployment documentation configures and verifies the three-day R2 Lifecycle rule for export/partial prefixes in every environment.
    - The deployed public-bucket behavior and its accepted direct-object exposure are documented without describing it as private or best practice.
    - Actual backup retention is recorded, suppression-marker retention covers that period, and the restore runbook applies suppressions before authentication is enabled.
    - Representative English and Spanish ZIPs pass manual ZIP, JSON, and spreadsheet inspection.
    - Redis primary/backup, R2, PostgreSQL-failure, token invalidation, responsive-dialog, and backup-restore manual checks are recorded.
    - Full backend tests/style checks and full frontend tests/lint/build pass.
    - Confirmed external-provider, no-reauthentication, no-audit, identifier-reuse, notification, and residual-TTL risks remain visible.
  - Validation:
    - Follow every automated and manual command/check in the spec Testing Plan.
  - Notes: this ticket is a release-readiness slice, not permission to silently redesign accepted risks; any remediation requires a new product decision or spec revision.
