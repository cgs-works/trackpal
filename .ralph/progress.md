## Iteration 1 — GitHub issue #123

- **Selection rationale:** #123 was the first unfinished item in the required sequence (`#123`, `#124`, `#125`, `#112`). Its live issue body was read with `gh issue view 123 --comments`; it depends on the Demo Tenant persistence foundation already present in the repository.
- **Decisions and assumptions:** Kept the existing REST authentication surface and added an authenticated `GET|POST /api/v1/auth/heartbeat` lifecycle endpoint. Demo metadata is exposed as top-level authentication response fields only; no workspace data is returned. `demo_ended` uses HTTP 410, and `demo_credentials_replaced` uses HTTP 401. Existing production authentication response behavior remains compatible. No external service or credential was required.
- **Changed files for #123:**
  - `backend/app/api/dependencies.py`
  - `backend/app/api/v1/endpoints/auth.py`
  - `backend/app/core/security.py`
  - `backend/app/schemas/auth.py`
  - `backend/app/services/auth_service/service.py`
  - `backend/app/services/demo_lifecycle_service.py`
  - `backend/app/services/profile_service/service.py`
  - `backend/tests/conftest.py`
  - `backend/tests/test_auth.py`
  - `docs/architecture/api-layer.md`
  - `docs/project-pdr/business-rules.md`
- **Implementation:** Added atomic 48-hour activation, authoritative lifecycle/version checks, lifecycle-only heartbeat metadata, Demo credential-version claims, refresh-session revocation and stable replacement errors, expiry deletion on the first relevant request, and production authentication regression coverage. Added HTTP integration tests for failed login, activation, concurrent login, refresh/logout preservation, heartbeat, replacement, expiry, and production responses.
- **Review result:** Completed a manual two-axis review against issue #123 and the backend conventions. No issue-specific standards or scope findings remained. `git diff --check` passed; targeted Ruff checks passed.
- **Verification commands and results:**
  - `cd backend && uv run pytest tests/test_auth.py tests/test_profile.py -q` — **68 passed**.
  - `cd backend && uv run ruff check app/api/dependencies.py app/api/v1/endpoints/auth.py app/core/security.py app/schemas/auth.py app/services/auth_service/service.py app/services/demo_lifecycle_service.py app/services/profile_service/service.py tests/conftest.py tests/test_auth.py` — **passed**.
  - `cd backend && uv run pytest` — **1677 passed, 1 skipped** (19 pre-existing warnings).
  - `cd frontend && npm test -- --run` — **109 passed**.
  - `cd frontend && npm run build` — **passed** (Vite emitted an existing chunk-size warning).
  - `cd frontend && npm run lint` — **failed on pre-existing errors** in `frontend/src/features/admin/components/mailbox-section.tsx`: `loadMailboxData` is accessed before declaration and React Compiler manual memoization cannot be preserved. No frontend source was changed for #123.
- **Blocker for the next fresh iteration:** The mandatory frontend lint verification gate is not green. Issue #123 remains `passes: false`; later Ralph items remain ineligible. The unrelated lint file was preserved rather than modified outside the selected issue.
