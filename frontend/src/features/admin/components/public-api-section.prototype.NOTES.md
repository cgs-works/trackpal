# Public API Section Prototype

Question: which tenant-facing layout makes the Public API Catalog settings understandable for non-technical tenants?

Prototype: `PublicApiSection` renders three throwaway variants on the existing `/admin/settings` route, switchable with `?variant=A|B|C` and the floating dev-only switcher.

Verdict: pending user review. Delete losing variants and this note once the winning layout is folded into production code.
